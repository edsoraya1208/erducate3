rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // Helper function to check if user owns the document
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // Safe lecturer check with proper null handling
    function isLecturer() {
      return isAuthenticated() &&
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.keys().hasAll(['role']) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'lecturer';
    }
    
    // Safe student check with proper null handling
    function isStudent() {
      return isAuthenticated() &&
             exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.keys().hasAll(['role']) &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'student';
    }
    
    // Check if user owns a specific class
    function ownsClass(classId) {
      return isAuthenticated() &&
             exists(/databases/$(database)/documents/classes/$(classId)) &&
             get(/databases/$(database)/documents/classes/$(classId)).data.instructorId == request.auth.uid;
    }
    
    // Users collection rules
    match /users/{userId} {
      allow create: if isAuthenticated() && isOwner(userId);
      allow update: if isAuthenticated() && isOwner(userId);
      allow read: if isAuthenticated();  // Any authenticated user can read user profiles
      allow delete: if false; // Users shouldn't delete their own accounts via app
    }

    // Classes collection rules
    match /classes/{classId} {
      allow create: if isLecturer();
      allow read: if isAuthenticated() && (
        (isLecturer() && resource.data.instructorId == request.auth.uid) ||
        isStudent()
      );
      allow update, delete: if isAuthenticated() && 
                              resource.data.instructorId == request.auth.uid;
    }
    
    // 🔧 FIXED: Student-Class enrollment collection - proper cascade delete support
    match /studentClasses/{enrollmentId} {
      allow create: if isAuthenticated() && 
                       isStudent() && 
                       request.resource.data.studentId == request.auth.uid;
      
      allow read: if isAuthenticated() && (
        (isStudent() && resource.data.studentId == request.auth.uid) ||
        (isLecturer() && ownsClass(resource.data.classId))
      );
      
      allow update: if isAuthenticated() && 
                       isLecturer() && 
                       ownsClass(resource.data.classId);
      
      // 🔧 FIXED: Allow students to leave class AND lecturers to remove students
      allow delete: if isAuthenticated() && (
        (isStudent() && resource.data.studentId == request.auth.uid) ||
        (isLecturer() && ownsClass(resource.data.classId))
      );
    }
    
    // Exercise rules - same as before
    match /classes/{classId}/exercises/{exerciseId} {
      allow create, update, delete: if isAuthenticated() && ownsClass(classId);
      allow read: if isAuthenticated() && (
        ownsClass(classId) ||
        isStudent()
      );
    }
    
    // 🔧 FIXED: Student progress - allow proper cleanup
    match /studentProgress/{progressId} {
      // Students can create their own progress
      allow create: if isAuthenticated() && 
                      isStudent() && 
                      request.resource.data.studentId == request.auth.uid;
      
      // Students can read/update their own progress
      allow read, update: if isAuthenticated() && 
                            isStudent() && 
                            resource.data.studentId == request.auth.uid;
      
      // Lecturers can read student progress for their classes  
      allow read: if isAuthenticated() && 
                    isLecturer() && 
                    exists(/databases/$(database)/documents/classes/$(resource.data.classId)) &&
                    get(/databases/$(database)/documents/classes/$(resource.data.classId)).data.instructorId == request.auth.uid;
      
      // 🔧 FIXED: Allow lecturers to delete progress when cleaning up exercises/classes
      allow delete: if isAuthenticated() && 
                      isLecturer() && 
                      exists(/databases/$(database)/documents/classes/$(resource.data.classId)) &&
                      get(/databases/$(database)/documents/classes/$(resource.data.classId)).data.instructorId == request.auth.uid;
    }
    
    // 🔧 FIXED: Submissions collection - allow proper cleanup  
    match /submissions/{submissionId} {
      // Students can create/update their own submissions
      allow create, update: if isAuthenticated() && 
                              isStudent() && 
                              request.resource.data.studentId == request.auth.uid;
      
      // Students can read their own submissions
      allow read: if isAuthenticated() && 
                    isStudent() && 
                    resource.data.studentId == request.auth.uid;
      
      // Lecturers can read submissions for their classes
      allow read: if isAuthenticated() && 
                    isLecturer() && 
                    exists(/databases/$(database)/documents/classes/$(resource.data.classId)) &&
                    get(/databases/$(database)/documents/classes/$(resource.data.classId)).data.instructorId == request.auth.uid;
      
      // Lecturers can update submissions for grading
      allow update: if isAuthenticated() && 
                      isLecturer() && 
                      exists(/databases/$(database)/documents/classes/$(resource.data.classId)) &&
                      get(/databases/$(database)/documents/classes/$(resource.data.classId)).data.instructorId == request.auth.uid;
      
      // 🔧 FIXED: Allow lecturers to delete submissions when cleaning up exercises/classes
      allow delete: if isAuthenticated() && 
                      isLecturer() && 
                      exists(/databases/$(database)/documents/classes/$(resource.data.classId)) &&
                      get(/databases/$(database)/documents/classes/$(resource.data.classId)).data.instructorId == request.auth.uid;
    }
    
    // Deny all other collections
    match /{document=**} {
      allow read, write: if false;
    }
  }
}